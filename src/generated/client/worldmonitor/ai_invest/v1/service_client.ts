// Code generated by protoc-gen-ts-client. DO NOT EDIT.
// source: worldmonitor/ai_invest/v1/service.proto

export interface ReasoningStep {
  step: number;
  category: string;
  description: string;
  evidence: string;
}

export interface InvestmentOpportunity {
  id: string;
  asset: string;
  action: string;
  confidence: number;
  rationale: string;
  dataSources: string[];
  timeHorizon: string;
  riskLevel: string;
  currentValue: string;
  category: string;
}

export interface GetInvestmentAnalysisRequest {
  marketSummary: string;
  newsSummary: string;
  economicSummary: string;
  focus: string;
  lang: string;
}

export interface GetInvestmentAnalysisResponse {
  reasoning: ReasoningStep[];
  opportunities: InvestmentOpportunity[];
  marketOutlook: string;
  model: string;
  generatedAt: number;
  cached: boolean;
  disclaimer: string;
  error: string;
}

export interface MarketSignal {
  type: string;
  sector: string;
  description: string;
  strength: number;
}

export interface GetMarketRadarRequest {
  headlines: string[];
  priceChanges: string;
  lang: string;
}

export interface GetMarketRadarResponse {
  signals: MarketSignal[];
  overallSentiment: string;
  summary: string;
  model: string;
  generatedAt: number;
  cached: boolean;
  error: string;
}

export interface MarketplaceListing {
  id: string;
  title: string;
  price: string;
  platform: string;
  location: string;
  category: string;
  url: string;
  imageUrl: string;
  sellerName: string;
  sellerPhone: string;
  sellerEmail: string;
}

export interface ArbitrageMatch {
  sellListing: MarketplaceListing;
  buyRequest: MarketplaceListing;
  estimatedProfit: string;
  profitPercent: number;
  aiNote: string;
}

export interface GetMarketplaceListingsRequest {
  query: string;
  country: string;
  lang: string;
}

export interface GetMarketplaceListingsResponse {
  sellListings: MarketplaceListing[];
  buyRequests: MarketplaceListing[];
  arbitrageMatches: ArbitrageMatch[];
  summary: string;
  model: string;
  generatedAt: number;
  cached: boolean;
  error: string;
}

// ── AI Dashboard types ──

export interface FearGreedData {
  value: number;
  classification: string;
  history: FearGreedHistoryEntry[];
}

export interface FearGreedHistoryEntry {
  value: number;
  classification: string;
  timestamp: number;
}

export interface TopMover {
  symbol: string;
  name: string;
  price: number;
  change24h: number;
  volume24h: number;
  marketCap: number;
  sparkline: number[];
  image: string;
}

export interface SectorPerformance {
  sector: string;
  change1d: string;
  change1w: string;
  change1m: string;
  change3m: string;
  changeYtd: string;
  change1y: string;
}

export interface ForexRate {
  pair: string;
  rate: number;
  change: string;
  bid: number;
  ask: number;
}

export interface CandleData {
  timestamp: number;
  open: number;
  high: number;
  low: number;
  close: number;
  volume: number;
}

export interface MarketStatusInfo {
  exchange: string;
  isOpen: boolean;
  holiday: string;
}

export interface EconomicIndicator {
  id: string;
  name: string;
  value: number;
  unit: string;
  change: number;
  trend: string;
  date: string;
}

export interface TechnicalIndicator {
  symbol: string;
  rsi: number;
  macdLine: number;
  macdSignal: number;
  macdHist: number;
  sma50: number;
  sma200: number;
  price: number;
}

export interface GetAiDashboardRequest {
  lang: string;
}

// ── Product Trends types ──

export interface ProductTrend {
  id: string;
  name: string;
  category: string;
  demandLevel: string;
  trendDirection: string;
  priceRange: string;
  monthlySearches: number;
  sparkline: number[];
  platforms: string[];
}

export interface ProductSupplier {
  id: string;
  productName: string;
  supplierName: string;
  platform: string;
  price: string;
  moq: string;
  location: string;
  url: string;
  rating: number;
  contactEmail: string;
  contactPhone: string;
}

export interface TrendCategory {
  category: string;
  demandScore: number;
  growth: string;
  topProduct: string;
}

export interface GetProductTrendsRequest {
  country: string;
  lang: string;
}

export interface GetProductTrendsResponse {
  trends: ProductTrend[];
  suppliers: ProductSupplier[];
  categories: TrendCategory[];
  summary: string;
  model: string;
  generatedAt: number;
  cached: boolean;
  error: string;
}

export interface GetAiDashboardResponse {
  fearGreed: FearGreedData;
  topMovers: TopMover[];
  btcCandles: CandleData[];
  forexRates: ForexRate[];
  sectors: SectorPerformance[];
  marketStatus: MarketStatusInfo[];
  economicIndicators: EconomicIndicator[];
  technicalIndicators: TechnicalIndicator[];
  generatedAt: number;
  cached: boolean;
  error: string;
}

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface AiInvestServiceClientOptions {
  fetch?: typeof fetch;
  defaultHeaders?: Record<string, string>;
}

export interface AiInvestServiceCallOptions {
  headers?: Record<string, string>;
  signal?: AbortSignal;
}

export class AiInvestServiceClient {
  private baseURL: string;
  private fetchFn: typeof fetch;
  private defaultHeaders: Record<string, string>;

  constructor(baseURL: string, options?: AiInvestServiceClientOptions) {
    this.baseURL = baseURL.replace(/\/+$/, "");
    this.fetchFn = options?.fetch ?? globalThis.fetch;
    this.defaultHeaders = { ...options?.defaultHeaders };
  }

  async getInvestmentAnalysis(req: GetInvestmentAnalysisRequest, options?: AiInvestServiceCallOptions): Promise<GetInvestmentAnalysisResponse> {
    let path = "/api/ai-invest/v1/get-investment-analysis";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "POST",
      headers,
      body: JSON.stringify(req),
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as GetInvestmentAnalysisResponse;
  }

  async getMarketRadar(req: GetMarketRadarRequest, options?: AiInvestServiceCallOptions): Promise<GetMarketRadarResponse> {
    let path = "/api/ai-invest/v1/get-market-radar";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "POST",
      headers,
      body: JSON.stringify(req),
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as GetMarketRadarResponse;
  }

  async getAiDashboard(req: GetAiDashboardRequest, options?: AiInvestServiceCallOptions): Promise<GetAiDashboardResponse> {
    let path = "/api/ai-invest/v1/get-ai-dashboard";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "POST",
      headers,
      body: JSON.stringify(req),
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as GetAiDashboardResponse;
  }

  async getMarketplaceListings(req: GetMarketplaceListingsRequest, options?: AiInvestServiceCallOptions): Promise<GetMarketplaceListingsResponse> {
    let path = "/api/ai-invest/v1/get-marketplace-listings";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "POST",
      headers,
      body: JSON.stringify(req),
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as GetMarketplaceListingsResponse;
  }

  async getProductTrends(req: GetProductTrendsRequest, options?: AiInvestServiceCallOptions): Promise<GetProductTrendsResponse> {
    let path = "/api/ai-invest/v1/get-product-trends";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "POST",
      headers,
      body: JSON.stringify(req),
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as GetProductTrendsResponse;
  }

  private async handleError(resp: Response): Promise<never> {
    const body = await resp.text();
    if (resp.status === 400) {
      try {
        const parsed = JSON.parse(body);
        if (parsed.violations) {
          throw new ValidationError(parsed.violations);
        }
      } catch (e) {
        if (e instanceof ValidationError) throw e;
      }
    }
    throw new ApiError(resp.status, `Request failed with status ${resp.status}`, body);
  }
}
